export enum MessageType {
    JOIN = "join",
    READY = "ready",
    START = "start",
    QUESTION = "question",
    ANSWER = "answer",
    RESULTS = "results",
    ELIMINATED = "eliminated",
    END = "end",
    ERROR = "error"
}